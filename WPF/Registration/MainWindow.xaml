<Window x:Class="Registration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Registration"
        mc:Ignorable="d"
        Title="MainWindow" Height="461.416" Width="434.672">
    <StackPanel x:Name="mainStackPanel">
        <TextBlock Margin="15" HorizontalAlignment="Center" Text="Sign up with your email address" FontWeight="Bold" FontSize="16"/>
        <TextBox Text="{Binding Username}"/>
        <TextBox Text="{Binding Password}"/>
        <TextBox Text="{Binding Email}"/>
        <TextBox Text="{Binding ConfirmEmail}"/>
        <DatePicker Text="{Binding Birthday}"/>
        <Button Content="SignUp" Click="ButtonSignUp_Click"/>

    </StackPanel>
</Window>
