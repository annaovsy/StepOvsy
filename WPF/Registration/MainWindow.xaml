<Window x:Class="Registration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Registration"
        mc:Ignorable="d"
        Title="Registration" Height="397.416" Width="434.672" WindowStartupLocation="CenterScreen">
    <StackPanel x:Name="mainStackPanel" Margin="0,0,0,2">
        <TextBlock Margin="15" HorizontalAlignment="Center" Text="Sign up with your email address" FontWeight="Bold" FontSize="16"/>
        <TextBox Style="{StaticResource textBox}" Text="{Binding Username}"/>
        <TextBox Style="{StaticResource textBox}" Text="{Binding Password}"/>
        <TextBox Style="{StaticResource textBox}" Text="{Binding Email}"/>
        <TextBox Style="{StaticResource textBox}" Text="{Binding ConfirmEmail}"/>
        <DatePicker Style="{StaticResource textBox}" Text="{Binding Birthday}"/>
        <Button Content="SIGN UP" Style="{StaticResource signInBtn}" Click="ButtonSignUp_Click"/>

    </StackPanel>
</Window>
